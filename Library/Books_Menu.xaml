<Window x:Class="Library.Books_Menu"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Library"
        mc:Ignorable="d"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}"
        Title="Books_Menu" Height="545.985" Width="868.181">
    
    <Grid>
        <Grid.Background>
            <ImageBrush ImageSource="Фон регистрации.jpg"/>
        </Grid.Background>
        <DataGrid x:Name="dgv_Books" ColumnWidth="*"  Height="505" VerticalAlignment="Bottom" Background="{x:Null}" Padding="0" FontSize="16" FontFamily="Bradley Hand ITC" HeadersVisibility="All" Margin="422,0,10,0"/>
        <ComboBox HorizontalAlignment="Left" Margin="232,146,0,0" VerticalAlignment="Top" Width="152"/>

    </Grid>
</Window>
